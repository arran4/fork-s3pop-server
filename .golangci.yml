run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - revive

issues:
  exclude-rules:
    - path: 'backend/backend\.go'
      linters:
        - staticcheck
      text: "SA1019"
